# pilot participant data — never commit (privacy promise to friends)
pilot-data/
*.bundle.json

# backend runtime data: user auth tokens + submitted telemetry bundles
backend/data/

# feature bundles generated by `vibescore report --out` (real telemetry)
# collector/ ignores all json EXCEPT package manifests
collector/*.json
!collector/package.json
!collector/package-lock.json

node_modules/
.env
.env.*
!.env.example
*.sqlite
*.sqlite-shm
*.sqlite-wal
.local/
