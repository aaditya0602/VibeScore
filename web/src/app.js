const app=document.querySelector('#app');
const accountNav=document.querySelector('#account-nav');
const toastEl=document.querySelector('#toast');
let session=null,catalog=null,timerId=null,saveId=null;
const h=value=>String(value??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const jsonHeaders={'content-type':'application/json'};
async function request(path,options={}){const response=await fetch(path,{credentials:'same-origin',...options,headers:{...(options.body?jsonHeaders:{}),...(options.headers||{})}});let data={};try{data=await response.json()}catch{}if(!response.ok)throw new Error(data.error||`Request failed (${response.status})`);return data;}
function toast(message,error=false){toastEl.textContent=message;toastEl.className=`toast show${error?' error':''}`;clearTimeout(toast.t);toast.t=setTimeout(()=>toastEl.className='toast',error?6000:3500)}
function menu(open){const b=document.querySelector('.nav-toggle');document.body.classList.toggle('menu-open',open);b.setAttribute('aria-expanded',String(open));b.setAttribute('aria-label',open?'Close menu':'Open menu')}
function navigate(path){menu(false);history.pushState({},'',path);window.scrollTo({top:0,left:0});render();}
function loading(label='Loading…'){app.innerHTML=`<div class="route-loading" role="status"><span aria-hidden="true"></span><p>${h(label)}</p></div>`}
function empty(title,text,action=''){return `<div class="empty"><strong>${h(title)}</strong><p>${h(text)}</p>${action}</div>`}
function updateNav(){accountNav.innerHTML=session?`<a class="button ghost small" href="/dashboard" data-link>${h(session.user.handle)}</a><button class="button small" data-action="logout">Sign out</button>`:'<a class="button ghost small" href="/login" data-link>Sign in</a><a class="button small" href="/join" data-link>Join beta</a>'}
async function loadSession(){try{session=await request('/api/me')}catch{session=null}updateNav()}
async function getCatalog(){if(!catalog)catalog=(await request('/api/challenges')).challenges;return catalog}
function page(title,body){document.title=`${title} — VibeScore`;document.body.classList.remove('in-workspace');document.querySelector('.site-header').hidden=false;document.querySelector('.site-footer').hidden=false;app.innerHTML=body;app.focus({preventScroll:true})}
function workPage(body){document.title='Challenge — VibeScore';document.body.classList.add('in-workspace');document.querySelector('.site-header').hidden=true;document.querySelector('.site-footer').hidden=true;app.innerHTML=body}
function metric(label,value){return `<div class="metric-line"><span>${h(label)}</span><div class="bar"><i style="--value:${Number(value)}%"></i></div><b>${Number(value)}</b></div>`}
function artifacts(items=[]){return items.length?`<section class="artifact-stack"><h3>Workspace files</h3>${items.map((x,i)=>`<details class="artifact" ${i===0?'open':''}><summary><span>${h(x.name)}</span><small>${h(x.language)}</small></summary><pre><code>${h(x.content)}</code></pre></details>`).join('')}</section>`:''}

function home(){page('Build better with AI',`<div class="shell"><section class="hero"><div class="hero-copy"><div class="eyebrow">The AI builder skill layer</div><h1>Build with AI.<br><span class="gradient-text">Prove you’re good at it.</span></h1><p>VibeScore turns real coding-agent work and controlled challenges into useful coaching, measurable progress, and a profile you can share.</p><div class="hero-actions"><a class="button" href="/join" data-link>Get your VibeScore →</a><a class="button ghost" href="/practice" data-link>Try a skill drill</a></div><div class="trust-row"><span>Raw prompts stay local</span><span>Transparent evidence</span><span>Public by choice</span></div></div><figure class="score-preview" aria-label="Sample VibeScore profile with illustrative numbers"><div class="score-orbit" aria-hidden="true"></div><div class="score-card"><div class="sample-label">Sample profile · illustrative data</div><div class="score-main"><div class="score-number">1742 <small>± 82</small></div><div class="rank-pill">TOP 12%</div></div><div class="mini-metrics">${metric('Direction',87)}${metric('Craft',76)}${metric('Efficiency',81)}${metric('Shipping',72)}</div></div></figure></section><section class="section"><div class="eyebrow">Two paths, one skill profile</div><h2 class="section-title">Measure your work.<br>Sharpen your process.</h2><p class="section-intro">Passive workflow evidence reveals your habits. Controlled practice gives you a fair place to improve and prove them.</p><div class="path-grid"><article class="card path-card"><span class="index">01 · REAL WORK</span><h3>Connect your coding-agent history</h3><p>Run the local MCP on a project you choose. It reduces Claude Code and Codex sessions to private numeric signals, then shows where your workflow helps or hurts.</p><a class="button ghost" href="/privacy" data-link>How private analysis works →</a></article><article class="card path-card"><span class="index">02 · CONTROLLED PRACTICE</span><h3>Train inside an AI-assisted arena</h3><p>Practise framing, context, debugging, verification, review, and efficiency. Take timed coding rounds with an assistant and objective tests.</p><a class="button ghost" href="/practice" data-link>Explore the arena →</a></article></div></section><section class="section"><div class="eyebrow">The loop</div><h2 class="section-title">Evidence that helps you improve.</h2><div class="feature-grid"><article class="card feature-card"><div class="icon-box">01</div><h3>Observe</h3><p>Private local telemetry highlights correction loops, context quality, tool use, verification, and delivery signals.</p></article><article class="card feature-card"><div class="icon-box">02</div><h3>Practise</h3><p>Your weakest dimension maps to focused drills and interview rounds with immediate, honest feedback.</p></article><article class="card feature-card"><div class="icon-box">03</div><h3>Prove</h3><p>Controlled attempts create a separate assessed rating for your profile and the global beta leaderboard.</p></article></div></section></div>`)}

function tab(active,attr,value,label){return `<button type="button" class="tab ${active?'active':''}" aria-pressed="${active}" ${attr}="${h(value)}">${label}</button>`}
async function practice(filter='all'){loading('Loading challenges…');const items=await getCatalog();const shown=filter==='all'?items:items.filter(x=>x.kind===filter);page('Practice',`<div class="shell"><header class="page-head"><div class="eyebrow">Skill arena</div><h1>Practice the process,<br>not trivia.</h1><p>Short drills strengthen how you direct and review AI. Timed coding rounds test whether you can use assistance and still ship correct work.</p></header><div class="tabs" role="group" aria-label="Filter challenges">${tab(filter==='all','data-filter','all',`All ${items.length}`)}${tab(filter==='drill','data-filter','drill',`Drills ${items.filter(x=>x.kind==='drill').length}`)}${tab(filter==='interview','data-filter','interview',`Interview rounds ${items.filter(x=>x.kind==='interview').length}`)}</div>${shown.length?`<div class="challenge-grid">${shown.map(challengeCard).join('')}</div>`:empty('Nothing here yet','New challenges in this category are on the way.')}</div>`)}
function challengeCard(c){return `<article class="card challenge-card"><div class="challenge-meta"><span class="skill">${h(c.skill)}</span><span>${h(c.difficulty)} · ${c.minutes} min</span></div><h3>${h(c.title)}</h3><p>${h(c.summary)}</p><a class="button ghost wide" href="/challenge/${encodeURIComponent(c.id)}" data-link>${c.kind==='drill'?'Open drill':'View interview'} →</a></article>`}
async function challengeDetail(id){loading('Loading challenge…');const {challenge:c}=await request(`/api/challenges/${encodeURIComponent(id)}`);page(c.title,`<div class="shell"><div class="challenge-detail"><article class="challenge-body"><div class="eyebrow">${h(c.kind)} · ${h(c.skill)}</div><h1>${h(c.title)}</h1><p>${h(c.summary)}</p><h2>Your brief</h2><p>${h(c.prompt)}</p><h2>What good work includes</h2><ul class="requirements">${c.requirements.map(x=>`<li>${h(x)}</li>`).join('')}</ul>${artifacts(c.artifacts)}</article><aside class="card side-panel"><dl><div><dt>Difficulty</dt><dd>${h(c.difficulty)}</dd></div><div><dt>Time</dt><dd>${c.minutes} min</dd></div><div><dt>Skill</dt><dd>${h(c.skill)}</dd></div><div><dt>Format</dt><dd>${h(c.artifacts?.length?'Workspace round':c.kind)}</dd></div></dl><button class="button wide" data-start="${h(c.id)}" data-mode="practice">Start practice</button>${c.kind==='interview'?`<button class="button ghost wide" style="margin-top:.6rem" data-start="${h(c.id)}" data-mode="rated">Start rated round</button>`:''}<p class="form-note">${c.kind==='interview'?'Rated interview timers begin immediately. Your first completed rated attempt on a task is eligible for the beta rating.':'Structured feedback is coaching evidence and does not affect the controlled challenge rating.'}</p></aside></div></div>`)}

function authPage(kind){const join=kind==='join',recover=kind==='recover';page(join?'Join the beta':recover?'Recover access':'Sign in',`<div class="auth-wrap"><div class="card auth-card"><div class="eyebrow">${join?'Create your profile':recover?'Account recovery':'Welcome back'}</div><h1>${join?'Start building better.':recover?'Use your recovery code.':'Sign in to VibeScore.'}</h1><p>${join?'Your profile stays private until you publish it.':recover?'Successful recovery replaces your old sessions and code.':'Continue your drills, attempts, and progress.'}</p><form class="form" id="auth-form" data-kind="${kind}"><div class="field"><label for="handle">Handle</label><input id="handle" name="handle" required minlength="2" maxlength="24" pattern="[a-z0-9]([a-z0-9_]|-)+" autocomplete="username" placeholder="ai_builder"${join?' aria-describedby="handle-hint"><small class="field-hint" id="handle-hint">2–24 lowercase letters, numbers, _ or -. This becomes your public handle if you publish.</small>':'>'}</div>${recover?'<div class="field"><label for="recovery">Recovery code</label><input id="recovery" name="recoveryCode" required autocomplete="off"></div>':''}<div class="field"><label for="password">${recover?'New password':'Password'}</label><input id="password" name="${recover?'newPassword':'password'}" type="password" required minlength="10" maxlength="128" autocomplete="${join||recover?'new-password':'current-password'}"${join||recover?' aria-describedby="password-hint"><small class="field-hint" id="password-hint">10–128 characters.</small>':'>'}</div><button class="button wide" type="submit">${join?'Create account':recover?'Recover account':'Sign in'} →</button><p class="form-note">${join?'Already have an account? <a href="/login" data-link>Sign in</a>':recover?'Remembered it? <a href="/login" data-link>Sign in</a>':'Lost access? <a href="/recover" data-link>Use your recovery code</a>'}</p></form></div></div>`)}
function showRecovery(code){page('Save recovery code',`<div class="auth-wrap"><div class="card auth-card"><div class="eyebrow">One important step</div><h1>Save this code.</h1><p>It is the only way to recover your account. We store only a one-way hash and cannot show it again.</p><div class="recovery-box"><code>${h(code)}</code></div><button class="button wide" style="margin-top:1rem" data-action="copy-recovery" data-code="${h(code)}">Copy recovery code</button><a class="button ghost wide" style="margin-top:.6rem" href="/dashboard" data-link>I saved it — continue</a></div></div>`)}

async function dashboard(){if(!session)return navigate('/login');loading('Loading your dashboard…');const [me,workflow]=await Promise.all([request('/api/me'),request('/api/workflow')]);session=me;updateNav();const s=session.summary,a=session.attempts;page('Dashboard',`<div class="shell"><header class="page-head"><div class="eyebrow">Your command center</div><h1>Welcome back,<br>${h(session.user.handle)}.</h1><p>Keep the loop moving: practise a weak skill, finish a controlled round, then compare the result with your real workflow.</p></header><div class="stats-grid"><div class="card stat-card"><strong>${h(s.rating??'—')}</strong><span>Challenge rating${s.rating==null?' · not established yet':''}</span></div><div class="card stat-card"><strong>${h(workflow.latest?.rating??'—')}</strong><span>Provisional workflow score${workflow.connected?'':' · not connected'}</span></div><div class="card stat-card"><strong>${s.completed}</strong><span>Completed attempts</span></div><div class="card stat-card"><strong>${session.user.isPublic?'Live':'Private'}</strong><span>Profile visibility</span></div></div><div class="dashboard-grid"><section class="card"><div class="challenge-meta"><span>Recent activity</span><a href="/practice" data-link>Find a challenge →</a></div><div class="activity">${a.length?a.slice(0,8).map(x=>`<a class="activity-row" href="/attempt/${h(x.id)}" data-link><span><b>${h(x.challengeId.replaceAll('-',' '))}</b><br><small class="muted">${new Date(x.startedAt).toLocaleDateString()} · ${h(x.mode)}</small></span><span class="status-chip ${h(x.status)}">${h(x.status)}</span></a>`).join(''):empty('No attempts yet','Your first drill takes about seven minutes and shows exactly what to sharpen next.','<a class="button small" href="/practice" data-link>Browse drills →</a>')}</div></section><aside class="card"><div class="eyebrow">Next move</div><h2>${workflow.connected?(s.ratedTasks<2?'Establish your challenge rating':'Strengthen your range'):'Connect your real work'}</h2><p class="muted">${workflow.connected?`${workflow.bundle.episodes} locally reduced episodes from ${h(workflow.bundle.agent)} are connected. Raw prompts and code stayed local.`:'Use the local MCP to grade selected Codex and Claude Code sessions, then publish only the numeric aggregate.'}</p><a class="button wide" href="${workflow.connected?'/practice':'/connect'}" data-link>${workflow.connected?'Choose a challenge':'Connect Codex + Claude'}</a><a class="button ghost wide" style="margin-top:.6rem" href="/settings" data-link>Profile settings</a></aside></div></div>`)}

async function attemptPage(id){if(!session)return navigate('/login');loading('Opening your workspace…');const data=await request(`/api/attempts/${encodeURIComponent(id)}`),a=data.attempt,c=data.challenge;if(a.status==='submitted')return resultPage(a,c);if(c.kind==='drill')return drillAttempt(a,c);return interviewAttempt(a,c,data.messages||[])}
function timer(deadline){clearInterval(timerId);const el=document.querySelector('#timer');if(!el)return;const update=()=>{const seconds=Math.max(0,Math.floor((Date.parse(deadline)-Date.now())/1000));el.textContent=`${String(Math.floor(seconds/60)).padStart(2,'0')}:${String(seconds%60).padStart(2,'0')}`;if(!seconds)clearInterval(timerId)};update();timerId=setInterval(update,1000)}
function drillAttempt(a,c){workPage(`<div class="workspace"><div class="work-top"><a href="/challenge/${h(c.id)}" data-link>← Exit</a><h1>${h(c.title)}</h1><span class="status-chip">${h(a.mode)}</span><span class="timer" id="timer" role="timer" aria-label="Time remaining"></span></div><div class="drill-workspace"><div class="eyebrow">${h(c.skill)} ${c.artifacts?.length?'scenario':'drill'}</div><h2>${h(c.prompt)}</h2><ul class="requirements">${c.requirements.map(x=>`<li>${h(x)}</li>`).join('')}</ul>${artifacts(c.artifacts)}<form id="drill-form"><div class="field"><label for="answer">${h(c.responseLabel||'Your instruction to the AI')}</label><textarea class="drill-answer" id="answer" name="answer" placeholder="Be specific about evidence, constraints, and success checks…">${h(a.answer)}</textarea></div><div class="hero-actions"><button class="button" type="submit">Check my response →</button><button class="button ghost" type="button" data-action="save-draft">Save draft</button></div><p class="form-note">When AI feedback is enabled, your answer is sent to this server’s configured AI provider for grading. Details are on the Privacy page.</p></form></div></div>`);timer(a.deadline)}
function interviewAttempt(a,c,messages){workPage(`<div class="workspace"><div class="work-top"><a href="/challenge/${h(c.id)}" data-link>← Exit</a><h1>${h(c.title)}</h1><span class="status-chip">${h(a.mode)}</span><span class="timer" id="timer" role="timer" aria-label="Time remaining"></span></div><div class="work-grid"><section class="work-pane prompt-pane"><div class="pane-head">Brief + workspace</div><div class="content"><div class="eyebrow">${h(c.skill)}</div><h2>${h(c.prompt)}</h2><ul class="requirements">${c.requirements.map(x=>`<li>${h(x)}</li>`).join('')}</ul>${artifacts(c.artifacts)}${c.examples?.length?`<h3>Examples</h3>${c.examples.map(x=>`<div class="card"><code>${h(JSON.stringify(x.input))} → ${h(JSON.stringify(x.output))}</code><p>${h(x.explanation)}</p></div>`).join('')}`:''}<div id="run-results" class="results" aria-live="polite"></div></div></section><section class="work-pane solution-pane"><div class="pane-head">Solution · JavaScript</div><textarea id="code-editor" class="code-editor" spellcheck="false" aria-label="Code editor">${h(a.code||c.starterCode)}</textarea><label class="reflection-label" for="reflection">Ownership debrief · private and unscored</label><textarea id="reflection" class="reflection-editor" maxlength="8000" placeholder="Explain the invariant, one rejected AI suggestion, a remaining risk, and why your tests are sufficient.">${h(a.reflection)}</textarea><div class="editor-actions"><button class="button ghost small" type="button" data-action="run-code">Run tests</button><button class="button small" type="button" data-action="submit-code" data-mode="${h(a.mode)}">Submit solution</button></div></section><aside class="work-pane assistant-pane"><div class="pane-head">AI interview coach</div><div class="chat" id="chat" role="log" aria-live="polite" aria-label="Conversation with the interview coach">${messages.length?messages.map(m=>`<div class="message ${h(m.role)}">${h(m.content)}</div>`).join(''):'<div class="message assistant">I can help you clarify the task, reason through tradeoffs, debug, and verify. I won’t hand you a complete solution.</div>'}</div><form class="chat-form" id="chat-form"><textarea name="message" aria-label="Message the interview coach" placeholder="Ask for a hint…"></textarea><button class="button small" type="submit">Send</button></form><p class="chat-note">Your messages and current draft are sent to this server’s configured AI provider.</p></aside></div></div>`);timer(a.deadline);const editor=document.querySelector('#code-editor'),reflection=document.querySelector('#reflection');const draft=()=>{clearTimeout(saveId);saveId=setTimeout(()=>saveAttempt(a.id,{code:editor.value,reflection:reflection.value},false),1200)};editor.addEventListener('input',draft);reflection.addEventListener('input',draft);request('/api/status').then(s=>{const note=document.querySelector('.chat-note');if(note&&!s.ai?.enabled)note.textContent='The AI coach is not enabled on this server. You can still run tests and submit.'}).catch(()=>{})}
async function saveAttempt(id,payload,notify=true){await request(`/api/attempts/${id}`,{method:'PATCH',body:JSON.stringify(payload)});if(notify)toast('Draft saved')}
function resultPage(a,c){const r=a.result||{},score=r.totalScore??r.score??0,improvements=r.improvements||[],strengths=r.strengths||[];page('Attempt result',`<div class="shell"><section class="card result-hero"><div class="eyebrow" style="justify-content:center">Attempt complete</div><div class="result-score">${h(score)}${r.totalScore!==undefined||r.maxScore===100?'<small>%</small>':''}</div><h1>${h(c.title)}</h1><p>${h(r.overallFeedback||'Your result has been saved.')}</p><p class="muted">${h(r.notice||r.ratingNote||'')}</p><div class="hero-actions" style="justify-content:center"><a class="button" href="/practice" data-link>Keep practising</a><a class="button ghost" href="/dashboard" data-link>View dashboard</a></div></section>${strengths.length||improvements.length?`<section class="section coaching-grid">${strengths.length?`<article class="card"><div class="eyebrow">What worked</div><ul class="requirements">${strengths.map(x=>`<li>${h(x)}</li>`).join('')}</ul></article>`:''}${improvements.length?`<article class="card"><div class="eyebrow">Improve next</div><ul class="requirements">${improvements.map(x=>`<li>${h(x)}</li>`).join('')}</ul></article>`:''}</section>`:''}${r.criteria?`<section class="section"><h2 class="section-title">Rubric feedback</h2><div class="feature-grid">${r.criteria.map(x=>`<article class="card"><div class="challenge-meta"><span>${h(x.label)}</span><b>${x.pointsEarned}/${x.pointsPossible}</b></div><span class="status-chip ${h(x.status||'')}">${h(x.status||'reviewed')}</span><p>${h(x.feedback)}</p>${x.evidence?`<blockquote class="evidence">${h(x.evidence)}</blockquote>`:''}</article>`).join('')}</div></section>`:''}${r.visibleTests?`<section class="section"><h2 class="section-title">Test evidence</h2><div class="card">${r.visibleTests.map(x=>`<div class="test-row"><span>${h(x.label)}</span><b class="${x.passed?'pass':'fail'}">${x.passed?'Passed':'Failed'}</b></div>`).join('')}<p class="muted">Hidden tests passed: ${r.hiddenTestsPassed}/${r.hiddenTestsTotal}</p></div></section>`:''}${r.reflection?`<section class="section"><h2 class="section-title">Ownership debrief</h2><article class="card"><p class="muted">${h(r.reflection.notice)}</p>${r.reflection.checks.map(x=>`<div class="test-row"><span>${h(x.label)}</span><b class="${x.met?'pass':'fail'}">${x.met?'Covered':'Add this'}</b></div>`).join('')}${r.reflection.improvements.length?`<ul class="requirements">${r.reflection.improvements.map(x=>`<li>${h(x)}</li>`).join('')}</ul>`:''}</article></section>`:''}</div>`)}

async function leaderboard(type='challenge'){loading('Loading the leaderboard…');const data=await request(`/api/leaderboard?type=${type}`);page('Leaderboard',`<div class="shell"><header class="page-head"><div class="eyebrow">Public beta ladder</div><h1>Skill earns the rank.</h1><p>Challenge ratings use controlled first attempts. Imported workflow scores remain separate and provisional.</p></header><div class="tabs" role="group" aria-label="Leaderboard type">${tab(type==='challenge','data-board','challenge','Challenge rating')}${tab(type==='workflow','data-board','workflow','Workflow score')}</div>${data.entries.length?`<div class="card leaderboard"><div class="leader-row head" aria-hidden="true"><span>Rank</span><span>Builder</span><span>Score</span><span>Evidence</span></div>${data.entries.map((x,i)=>{const evidence=type==='challenge'?`${x.ratedTasks} tasks`:`${x.episodes} episodes`;return `<a class="leader-row" href="/profile/${encodeURIComponent(x.handle)}" data-link aria-label="Rank ${i+1}: ${h(x.handle)}, score ${h(x.rating??'not set')}, ${h(evidence)}"><span class="rank">#${i+1}</span><span class="leader-name">${h(x.handle)}</span><span class="leader-score">${h(x.rating??'—')}</span><span class="muted">${h(evidence)}</span></a>`}).join('')}</div>`:empty('No ranked builders yet',type==='challenge'?'Complete a rated interview round and publish your profile to claim the first spot.':'Connect your coding-agent workflow and publish your profile to appear here.',`<a class="button small" href="${type==='challenge'?'/practice':'/connect'}" data-link>${type==='challenge'?'Find a rated round':'Connect your work'} →</a>`)}</div>`)}
async function profile(handle){loading('Loading profile…');const {profile:p}=await request(`/api/profile/${encodeURIComponent(handle)}`);const rating=p.challenge.rating??p.workflow?.rating??'—';page(`${p.handle}'s profile`, `<div class="shell"><section class="profile-hero"><div class="page-head"><div class="eyebrow">Public builder profile</div><h1>@${h(p.handle)}</h1><p>Two evidence lanes tell the honest story: controlled challenge results and provisional real-work signals.</p></div><div class="profile-rating"><div><strong>${h(rating)}</strong><span>${p.challenge.rating!=null?'Challenge rating':p.workflow?.rating!=null?'Workflow score':'No rating yet'}</span></div></div></section><div class="path-grid"><article class="card"><div class="challenge-meta"><span>Assessed challenges</span><span>${p.challenge.eligible?'Ranked':'Building evidence'}</span></div><h2>${h(p.challenge.rating??'Not established')}</h2><p class="muted">${h(p.challenge.ratedTasks)} rated tasks · ${h(p.challenge.completed)} completed attempts</p></article><article class="card"><div class="challenge-meta"><span>Imported workflow</span><span>Provisional</span></div><h2>${h(p.workflow?.rating??'Not connected')}</h2><p class="muted">${p.workflow?`${h(p.workflow.episodes)} episodes · ${h(p.workflow.agent)}`:`@${h(p.handle)} has not published a workflow report yet. This lane appears once they connect their coding agent and publish numeric evidence.`}</p></article></div></div>`)}

async function settings(){if(!session)return navigate('/login');loading('Loading settings…');session=await request('/api/me');page('Settings',`<div class="shell"><header class="page-head"><div class="eyebrow">Account controls</div><h1>Settings.</h1><p>Your data stays private until you choose otherwise. Deleting your account removes its attempts, messages, reports, and scores.</p></header><div class="dashboard-grid"><section class="card"><h2>Public profile</h2><p class="muted">${session.user.isPublic?'Your profile and eligible scores are visible on the public leaderboard.':'Your account, attempts, and reports are private.'}</p><button class="button ${session.user.isPublic?'ghost':''}" data-action="visibility" data-value="${!session.user.isPublic}">${session.user.isPublic?'Make profile private':'Publish my profile'}</button>${session.user.isPublic?`<a class="button ghost wide" style="margin-top:.7rem" href="/profile/${encodeURIComponent(session.user.handle)}" data-link>View public profile</a>`:''}<a class="button ghost wide" style="margin-top:.7rem" href="/connect" data-link>Connect Codex + Claude</a></section><section class="card"><h2>Delete account</h2><p class="muted">This permanently removes your beta data.</p><form id="delete-form" class="form"><div class="field"><label for="confirm-handle">Type <b>${h(session.user.handle)}</b> to confirm</label><input id="confirm-handle" name="confirmHandle" required autocomplete="off" spellcheck="false"></div><button class="button danger" type="submit">Delete my account</button></form></section></div></div>`)}

async function connectWork(){if(!session)return navigate('/login');loading('Loading your connection…');const data=await request('/api/workflow'),o=data.bundle?.overall;page('Connect your AI work',`<div class="shell"><header class="page-head"><div class="eyebrow">Private local evidence</div><h1>Connect Codex<br>and Claude Code.</h1><p>The VibeScore MCP finds project-matched sessions only when you ask, reduces them locally, previews the exact numeric upload, and requires confirmation before publishing.</p></header>${data.connected?`<section class="card workflow-score"><div><div class="eyebrow">Latest provisional score</div><h2>${h(data.latest?.rating??'Calculated')}</h2><p>${h(data.bundle.agent)} · ${o.episodes} episodes · generated ${h(new Date(data.bundle.generatedAt).toLocaleString())}</p></div><div class="mini-metrics">${metric('Context',Math.round(o.firstPromptContextScore*100))}${metric('Verify',Math.round(o.verifyAfterEditRatio*100))}${metric('Recovery',Math.round(o.errorRecoveryRate*100))}${metric('Agentic',Math.round(o.agenticLeverage*100))}</div></section>`:empty('No workflow report yet','Complete the three steps below, then ask your coding agent to run the VibeScore workflow. Your score appears here after you approve a publish.')}<div class="connect-grid"><section class="card"><div class="index">01 · INSTALL</div><h2>Run the local MCP</h2><p class="muted">Clone this repository, install dependencies, and use the absolute path to <code>collector/src/mcp.ts</code> in your MCP client.</p><pre><code>git clone https://github.com/aaditya0602/VibeScore.git
cd VibeScore
npm install</code></pre></section><section class="card"><div class="index">02 · AUTHORIZE</div><h2>Create a connection token</h2><p class="muted">The token can publish aggregate workflow evidence to your account. It is shown once and replacing it revokes the previous token.</p><button class="button" data-action="generate-api-token">Generate connection token</button></section></div><section id="connection-secret" class="connection-secret" aria-live="polite"></section><section class="card"><div class="index">03 · RUN</div><h2>Ask your coding agent</h2><ol class="requirements"><li>Find project sessions for this repository.</li><li>Analyze the Codex and Claude Code sessions you select.</li><li>Explain the report and recommend drills.</li><li>Preview the publish payload and show its SHA-256.</li><li>Publish only after you approve that exact preview.</li></ol><p class="form-note">Workflow scores are provisional usage evidence. They remain separate from controlled interview ratings.</p></section></div>`)}

function showConnectionToken(token){const origin=location.origin,handle=session.user.handle,path='C:\\ABSOLUTE_PATH\\VibeScore\\collector\\src\\mcp.ts';const codex=`[mcp_servers.vibescore]\ncommand = "node"\nargs = ["${path.replaceAll('\\','\\\\')}"]\nenv = { VIBESCORE_SERVER = "${origin}", VIBESCORE_HANDLE = "${handle}", VIBESCORE_TOKEN = "${token}" }`;const claude=JSON.stringify({mcpServers:{vibescore:{command:'node',args:[path],env:{VIBESCORE_SERVER:origin,VIBESCORE_HANDLE:handle,VIBESCORE_TOKEN:token}}}},null,2);document.querySelector('#connection-secret').innerHTML=`<div class="eyebrow">Shown once</div><h2>Save your MCP configuration now.</h2><p class="muted">Replace the example Windows path with the absolute path on your computer. Keep this token private.</p><div class="config-grid"><article class="card"><div class="challenge-meta"><span>Codex config.toml</span><button class="button ghost small" data-copy-target="codex-config">Copy</button></div><pre id="codex-config"><code>${h(codex)}</code></pre></article><article class="card"><div class="challenge-meta"><span>Claude Code .mcp.json</span><button class="button ghost small" data-copy-target="claude-config">Copy</button></div><pre id="claude-config"><code>${h(claude)}</code></pre></article></div>`;document.querySelector('#connection-secret').scrollIntoView({behavior:'smooth'})}
function privacy(){page('Privacy',`<div class="shell"><header class="page-head"><div class="eyebrow">Local first by design</div><h1>Your code is yours.</h1><p>A short, plain account of what VibeScore sees, where it goes, and what you control.</p></header><article class="privacy-copy"><ul class="privacy-summary"><li><b>Never leaves your machine:</b> raw prompts, source code, commands, file paths, and filenames from your coding-agent sessions.</li><li><b>Sent to the AI provider:</b> interview-coach messages and drill answers, only when the AI coach is enabled.</li><li><b>Never sent to the AI provider:</b> your workflow telemetry, which is numeric-only.</li></ul><h2>What stays on your machine</h2><p>The VibeScore MCP analyzes only project and session files you explicitly select. Prompt text, source code, commands, file paths, and filenames are reduced locally to numeric signals and are never included in publishable reports.</p><h2>What you can publish</h2><p>Publishing workflow evidence is opt-in. The MCP first shows the exact numeric bundle and its SHA-256 digest. Upload requires a second call with that exact digest and your explicit confirmation.</p><h2>AI coach and drill feedback</h2><p>When the AI coach is enabled on this server, the messages you send to the interview coach (with the challenge brief and your current draft code for that attempt) and your drill answers are sent to the AI provider the operator has configured, so it can reply and give feedback. On some providers’ free tiers, the provider may use request content to improve its products, so do not paste secrets or personal data into the coach or your answers. Workflow telemetry is numeric-only and is never sent to the AI provider.</p><h2>What the server stores</h2><p>Accounts, challenge attempts, coach messages, numeric reports, score history, and feedback. Passwords, recovery codes, and connection tokens are stored as one-way hashes. Profiles are private by default.</p><h2>What the scores mean</h2><p>Imported workflow scores are provisional self-reported evidence. Controlled challenge ratings come from server-run tests, but are not identity-proctored hiring credentials. We keep them separate.</p><h2>Your controls</h2><p>You can make your profile private or delete your account and its associated beta data at any time from <a href="/settings" data-link>Settings</a>.</p></article></div>`)}
async function check(){loading('Loading the readiness check…');await getCatalog();page('AI Builder Readiness Check',`<div class="shell"><header class="page-head"><div class="eyebrow">AI Builder Readiness Check</div><h1>What kind of<br>AI builder are you?</h1><p>Three questions, about two minutes, no account needed. You get a builder archetype and one focused drill to start with.</p></header><div class="check-layout"><section class="card check-banner" id="check-quiz" aria-live="polite"></section><aside class="card check-aside"><div class="eyebrow">What it looks at</div><ul class="requirements"><li><b>Verification</b> — what you do after the AI produces something plausible.</li><li><b>Recovery</b> — how you respond when an agent gets stuck.</li><li><b>Framing</b> — what goes into a strong first prompt.</li></ul><p class="form-note">Your answers stay in this browser tab. Nothing is stored or sent.</p></aside></div></div>`);quizStep(0,0,false)}

const quiz=[{q:'When your coding assistant produces a plausible solution, what do you do next?',a:[['Ship it while the context is fresh',0],['Read the main path and fix anything obvious',1],['Run focused tests, inspect edge cases, and review the diff',2]]},{q:'When a coding agent fails twice on the same issue, what is your next move?',a:[['Ask it to try again',0],['Switch models immediately',1],['Stop, summarize evidence, and change the hypothesis',2]]},{q:'What makes a strong first prompt?',a:[['A detailed list of technologies',1],['The user outcome, constraints, evidence, and success checks',2],['A short command so the model has freedom',0]]}];
const archetypes=[[5,'The Systems Builder','efficiency','You already verify, reset stuck loops, and frame outcomes. Your edge now is getting there with less back-and-forth.'],[3,'The Fast Iteration Builder','verification','You move quickly and catch the obvious problems. Tightening how you check AI output is the fastest way to raise your ceiling.'],[0,'The Instinctive Builder','framing','You trust the flow and let the model run. Clearer first prompts will save you the most rework.']];
function quizStep(index,points,focus=true){const box=document.querySelector('#check-quiz');if(!box)return;if(index>=quiz.length){const [,type,skill,copy]=archetypes.find(([min])=>points>=min),match=(catalog||[]).find(x=>x.kind==='drill'&&x.skill===skill);box.innerHTML=`<div class="challenge-meta"><span>Your result · ${points} of ${quiz.length*2} points</span><span>Readiness check</span></div><div class="bar quiz-progress"><i style="--value:100%"></i></div><h2 id="quiz-heading" tabindex="-1">${h(type)}</h2><p>${h(copy)} Your recommended starting focus is <b>${h(skill)}</b>.</p>${match?`<article class="card recommend-card"><div class="challenge-meta"><span class="skill">Recommended drill · ${h(match.skill)}</span><span>${h(match.difficulty)} · ${h(match.minutes)} min</span></div><h3>${h(match.title)}</h3><p class="muted">${h(match.summary)}</p></article>`:''}<div class="hero-actions">${match?`<a class="button" href="/challenge/${encodeURIComponent(match.id)}" data-link>Start this drill →</a>`:'<a class="button" href="/practice" data-link>Start practising →</a>'}${session?'<a class="button ghost" href="/practice" data-link>Browse all drills</a>':'<a class="button ghost" href="/join" data-link>Join to save your progress</a>'}<button class="button ghost" type="button" data-quiz="0" data-total="0">Retake the check</button></div>`}else{const item=quiz[index];box.innerHTML=`<div class="challenge-meta"><span>Question ${index+1} of ${quiz.length}</span><span>Readiness check</span></div><div class="bar quiz-progress" role="progressbar" aria-label="Check progress" aria-valuemin="0" aria-valuemax="${quiz.length}" aria-valuenow="${index}"><i style="--value:${Math.round(index/quiz.length*100)}%"></i></div><h2 id="quiz-heading" tabindex="-1">${h(item.q)}</h2><div class="quiz-options" role="group" aria-labelledby="quiz-heading">${item.a.map(([label,p])=>`<button type="button" class="quiz-option" data-quiz="${index+1}" data-total="${points+Number(p)}">${h(label)}</button>`).join('')}</div>`}if(focus)box.querySelector('#quiz-heading').focus()}
function notFound(){page('Page not found',`<div class="shell"><div class="error-page"><div class="error-code" aria-hidden="true">404</div><h1>Page not found.</h1><p class="muted">Nothing lives at <code>${h(location.pathname)}</code>. It may have moved, or the link may be mistyped.</p><div class="hero-actions"><a class="button" href="/" data-link>Back to home</a><a class="button ghost" href="/practice" data-link>Browse practice</a><a class="button ghost" href="/check" data-link>Take the readiness check</a></div></div></div>`)}

async function render(){clearInterval(timerId);const path=location.pathname;try{if(path==='/')return await home();if(path==='/practice')return await practice();if(path==='/login'||path==='/join'||path==='/recover')return await authPage(path.slice(1));if(path==='/dashboard')return await dashboard();if(path==='/connect')return await connectWork();if(path==='/leaderboard')return await leaderboard();if(path==='/settings')return await settings();if(path==='/privacy')return await privacy();if(path==='/check')return await check();if(path.startsWith('/challenge/'))return await challengeDetail(decodeURIComponent(path.slice(11)));if(path.startsWith('/attempt/'))return await attemptPage(decodeURIComponent(path.slice(9)));if(path.startsWith('/profile/'))return await profile(decodeURIComponent(path.slice(9)));return await notFound()}catch(error){const missing=/not found/i.test(error.message);page(missing?'Not found':'Something went wrong',`<div class="shell"><div class="error-page" role="alert"><div class="error-code" aria-hidden="true">${missing?'404':'!'}</div><h1>${missing?'Nothing to see here.':'We hit a snag.'}</h1><p class="muted">${h(error.message)}${missing?' It may be private, deleted, or mistyped.':''}</p><div class="hero-actions">${missing?'':'<button class="button" type="button" data-action="retry">Try again</button>'}<a class="button${missing?'':' ghost'}" href="/" data-link>Back to home</a>${missing?'<a class="button ghost" href="/leaderboard" data-link>View leaderboard</a>':''}</div></div></div>`)}finally{document.querySelectorAll('.nav a').forEach(a=>a.classList.toggle('active',a.getAttribute('href')===path))}}

document.addEventListener('click',async event=>{const link=event.target.closest('[data-link]');if(link&&link.origin===location.origin){event.preventDefault();navigate(link.getAttribute('href'));return}const target=event.target.closest('button');if(document.body.classList.contains('menu-open')&&!event.target.closest('.site-header'))menu(false);if(!target)return;try{
  if(target.dataset.action==='menu')return menu(!document.body.classList.contains('menu-open'));
  if(target.dataset.filter)return practice(target.dataset.filter);
  if(target.dataset.board)return leaderboard(target.dataset.board);
  if(target.dataset.start){if(!session)return navigate('/login');target.disabled=true;const data=await request(`/api/challenges/${encodeURIComponent(target.dataset.start)}/start`,{method:'POST',body:JSON.stringify({mode:target.dataset.mode})});navigate(`/attempt/${data.attempt.id}`)}
  if(target.dataset.action==='logout'){await request('/api/logout',{method:'POST'});session=null;updateNav();navigate('/')}
  if(target.dataset.action==='copy-recovery'){await navigator.clipboard.writeText(target.dataset.code);toast('Recovery code copied')}
  if(target.dataset.action==='retry')render();
  if(target.dataset.action==='save-draft'){const id=location.pathname.slice(9);await saveAttempt(id,{answer:document.querySelector('#answer')?.value,code:document.querySelector('#code-editor')?.value})}
  if(target.dataset.action==='run-code'){target.disabled=true;const id=location.pathname.slice(9),data=await request(`/api/attempts/${id}/run`,{method:'POST',body:JSON.stringify({code:document.querySelector('#code-editor').value})});document.querySelector('#run-results').innerHTML=`<h3>Visible tests</h3>${data.results.map(x=>`<div class="run-result"><div class="test-row"><span>${h(x.id)}</span><b class="${x.passed?'pass':'fail'}">${x.passed?'Passed':'Failed'}</b></div>${!x.passed?`<p class="muted">${h(x.error||`Actual output: ${JSON.stringify(x.actual)}`)}</p>`:''}</div>`).join('')}`;target.disabled=false}
  if(target.dataset.action==='submit-code'){const warning=target.dataset.mode==='rated'?'Submit this rated solution? Your first completed rated attempt cannot be repeated for rating credit.':'Submit this practice solution and view its feedback?';if(!confirm(warning))return;target.disabled=true;const id=location.pathname.slice(9),data=await request(`/api/attempts/${id}/submit`,{method:'POST',body:JSON.stringify({code:document.querySelector('#code-editor').value,reflection:document.querySelector('#reflection')?.value||''})});toast(`Submitted: ${data.result.totalScore}%`);render()}
  if(target.dataset.action==='visibility'){const data=await request('/api/me',{method:'PATCH',body:JSON.stringify({isPublic:target.dataset.value==='true'})});session.user=data.user;toast(data.user.isPublic?'Profile published':'Profile is private');settings()}
  if(target.dataset.action==='generate-api-token'){target.disabled=true;const data=await request('/api/me/api-token',{method:'POST'});showConnectionToken(data.token);target.remove()}
  if(target.dataset.copyTarget){const value=document.querySelector(`#${target.dataset.copyTarget}`)?.textContent||'';await navigator.clipboard.writeText(value);toast('Configuration copied')}
  if(target.dataset.quiz!==undefined){quizStep(Number(target.dataset.quiz),Number(target.dataset.total??target.dataset.points??0))}
}catch(error){target.disabled=false;toast(error.message,true)}});

document.addEventListener('submit',async event=>{event.preventDefault();const form=event.target;try{
  if(form.id==='auth-form'){const values=Object.fromEntries(new FormData(form)),button=form.querySelector('button[type="submit"]');const kind=form.dataset.kind,path=kind==='join'?'/api/register':kind==='recover'?'/api/recover':'/api/login';button.disabled=true;let data;try{data=await request(path,{method:'POST',body:JSON.stringify(values)})}finally{button.disabled=false}await loadSession();if(data.recoveryCode)return showRecovery(data.recoveryCode);navigate('/dashboard')}
  if(form.id==='drill-form'){const id=location.pathname.slice(9),answer=form.answer.value,button=form.querySelector('button[type="submit"]');button.disabled=true;button.textContent='Analyzing your reasoning…';try{await saveAttempt(id,{answer},false);await request(`/api/attempts/${id}/submit`,{method:'POST',body:JSON.stringify({answer})});render()}catch(error){button.disabled=false;button.textContent='Check my response →';throw error}}
  if(form.id==='chat-form'){const input=form.message,message=input.value.trim();if(!message)return;input.value='';const chat=document.querySelector('#chat');chat.insertAdjacentHTML('beforeend',`<div class="message user">${h(message)}</div>`);const id=location.pathname.slice(9),send=form.querySelector('button');send.disabled=true;chat.setAttribute('aria-busy','true');let data;try{data=await request(`/api/attempts/${id}/chat`,{method:'POST',body:JSON.stringify({message})})}finally{send.disabled=false;chat.removeAttribute('aria-busy')}chat.insertAdjacentHTML('beforeend',`<div class="message assistant">${h(data.message)}</div>`);chat.scrollTop=chat.scrollHeight}
  if(form.id==='delete-form'){const confirmHandle=form.confirmHandle.value;if(!confirm('Permanently delete this account and all beta data?'))return;await request('/api/me',{method:'DELETE',body:JSON.stringify({confirmHandle})});session=null;updateNav();navigate('/')}
}catch(error){toast(error.message,true)}});
window.addEventListener('popstate',()=>{menu(false);render()});
document.addEventListener('keydown',event=>{if(event.key==='Escape'&&document.body.classList.contains('menu-open')){menu(false);document.querySelector('.nav-toggle').focus()}});
await loadSession();render();
